name: CRYPTIX-MD
services:
  - name: CRYPTIX-MD services
    type: web
    ports:
      - 5000
    routes:
      - path: "/"
    build:
      type: docker
      dockerfile: ./Dockerfile
    regions:
      - fra
      - sin
      
